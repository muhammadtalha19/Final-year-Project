# fyp-ml-api

Small FastAPI demo container for the orchestrator. It simulates model inference with deterministic logic and exposes:

- `GET /`
- `GET /health`
- `POST /predict`
- `GET /docs`

Port: `8000`

Example image target used by the YAML examples:

```text
dockertalha19/fyp-ml-api:latest
```
