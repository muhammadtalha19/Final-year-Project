app:
  name: broken-app
  environment: production

deployment:
  type: container
  port: 80
  replicas: 1

requirements:
  max_monthly_cost_usd: 20
  min_uptime_percent: 99.9
  preferred_region: asia
  public_access: true
